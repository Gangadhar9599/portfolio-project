# Updated Spring Mail Properties - NOW USING NEW ENVIRONMENT VARIABLES

# FIX 1: Change port to 465 and configure fallback value for local testing
spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:465} 
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}

spring.mail.properties.mail.smtp.auth=true
# FIX 2: Disable STARTTLS on port 465 (Deployment Fix)
spring.mail.properties.mail.smtp.starttls.enable=false

# FIX 3: Reference the new SSL environment variable (Deployment Fix)
spring.mail.properties.mail.smtp.ssl.enable=${MAIL_SMTP_SSL_ENABLE}

# Recipient Email
portfolio.email.recipient=gingale763@gmail.com